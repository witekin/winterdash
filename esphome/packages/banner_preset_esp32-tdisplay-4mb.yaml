# GENERATED by esphome/tools/gen_board_assets.py from board_assets.yaml[esp32-tdisplay-4mb] - DO NOT EDIT.
# banner_preset picker (options = web set + Custom) + the on-screen lookup chains (screen set).
# The select id is defined ONCE here (per board). Order is canonical/append-only (NVS index restore).
substitutions:
  banner_dash_lookup: ""
  banner_saver_lookup: ""

select:
  - platform: template
    id: banner_preset
    name: "Banner preset"
    disabled_by_default: true
    optimistic: true
    restore_value: true
    options:
      - "Battery"
      - "Custom"
    initial_option: "Battery"
